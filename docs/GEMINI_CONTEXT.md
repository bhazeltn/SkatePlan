# Gemini Context

## Dependency Management

- **NEVER downgrade libraries.** Unless explicitly instructed to resolve a specific conflict, always maintain or upgrade the existing version numbers.
- **Preserve pinned versions.** If a version is pinned (e.g., `==5.2.8`), do not change it.
