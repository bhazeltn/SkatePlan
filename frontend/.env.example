# Base URL of the SkatePlan FastAPI backend.
# Leave empty to use same-origin requests (e.g. behind a shared reverse proxy).
# Example for local dev against the deployed backend:
# VITE_API_BASE_URL=https://ab12b61bf.abacusai.cloud
VITE_API_BASE_URL=
